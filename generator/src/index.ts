import type { ProjectDefinition } from "@backend-file-generator/shared";

const project:ProjectDefinition = {
    name: "Tushar",
    version: "v0",
    entities: [],
    relationships: [],
}

console.log(project)